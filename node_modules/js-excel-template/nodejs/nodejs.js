"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const XLSX = tslib_1.__importStar(require("xlsx"));
const common_1 = tslib_1.__importDefault(require("./common"));
/**
 * @public
 */
class JsExcelTemplate extends common_1.default {
    static fromFile(filepath) {
        const workbook = XLSX.readFile(filepath, {
            cellNF: true,
            cellStyles: true,
            cellDates: true
        });
        return new JsExcelTemplate(workbook);
    }
    toBuffer(bookType) {
        return XLSX.write(this.workbook, { bookType, type: 'buffer' });
    }
    saveAs(filepath) {
        XLSX.writeFile(this.workbook, filepath);
    }
}
exports.default = JsExcelTemplate;
